@model IEnumerable<FootballAGC.Models.PlayerPosition>

@{
    ViewData["Title"] = "Index";
}

<h1>Player Positions</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by position description: <input type="text" name="SearchString" />
            <input type="submit" value="Search" /> |
            <a asp-action="Index">Back to Full List</a>
        </p>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlayerPositionID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PositionDescription)
            </th>
            <th>Players</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerPositionID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PositionDescription)
            </td>
            <td>
                @{
                    foreach(var p in item.Players){
                        @p.PlayerID @: @p.FullName <br />
                    }
                }
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlayerPositionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlayerPositionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlayerPositionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
